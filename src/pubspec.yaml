name: ronda_stories
description: "Ronda Stories - Storie lungo il percorso"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_map: ^7.0.2
  flutter_map_compass: ^1.1.1
  latlong2: ^0.9.1
  flutter_tts: ^4.0.2
  geolocator: ^13.0.0
  wakelock_plus: ^1.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/circuits/ferrara-classico/
